#!/usr/bin/env bash

source ~/.os-setuprc &> /dev/null || ( echo "I can't read ~/.os-setuprc. Will not continue..." && exit 1 )

# create default passwords
for u in ${!user[*]}; do 
    htpasswd -bc /etc/openshift/openshift-passwd $u ${user[$u]};
done
